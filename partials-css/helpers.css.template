{{ define "helpers" -}}
.hidden {
  display: none;
}

.container {
  width: 60vw;
}

.center {
  display: flex;
  justify-content: center;
  align-items: center;
}

a.nolink,
a.nolink:hover,
a.nolink:active,
a.nolink:visited {
  text-decoration: none;
}
{{- end }}